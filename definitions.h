#pragma once

#include <iostream>
#include <fstream>
#include <array>
#include <vector>
#include <random>
#include <chrono>

#include "glm/glm.hpp"

typedef glm::vec3 vec3;
typedef glm::vec3 Vertex;
typedef glm::dvec3 Color;
typedef glm::vec3 Direction;

